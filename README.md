# Telegram Screenshot Bot

Telegram-бот для удалённого создания скриншотов экрана и отдельных окон Windows. Управляется через команды в чате, поддерживает интерактивный выбор окон кнопками.

## Возможности

- **Полный скриншот экрана** — снимок всех мониторов
- **Скриншот отдельного окна** — по названию или через интерактивные кнопки
- **Адаптивное сжатие** — автоматический подбор качества JPEG, чтобы влезть в заданный лимит трафика
- **Ретрай при таймаутах** — если Telegram режет трафик, бот сам пережмёт картинку и попробует снова
- **Контроль доступа** — whitelist пользователей по Telegram ID
- **Мультимонитор** — корректная работа с несколькими экранами
- **Устойчивый захват окон** — PrintWindow API + фолбэки через региональные скриншоты
- **Корректное завершение** — обработка закрытия консоли, очистка ресурсов

## Быстрый старт

### 1. Создайте бота

1. Найдите **@BotFather** в Telegram
2. Отправьте `/newbot`, выберите имя
3. Скопируйте токен

### 2. Узнайте свой Telegram ID

1. Напишите **@userinfobot** в Telegram
2. Отправьте `/start`
3. Скопируйте ваш ID

### 3. Настройте `settings.ini`

Создайте файл `settings.ini` рядом с ботом (или переименуйте `settings_exemple.ini`):

```ini
[telegram]
bot_token = 1234567890:ABCDEFghijklmnopqrstuvwxyz
allowed_users = 123456789, 987654321

[image]
# Максимальный размер картинки в КБ (по умолчанию 150)
max_size_kb = 150
```

### 4. Запустите

**Python:**
```bash
pip install -r requirements.txt
python screenshot_bot.py
```

**Или через pipenv:**
```bash
pipenv install
pipenv run python screenshot_bot.py
```

**Или готовый EXE** (см. [README_EXE.md](README_EXE.md)):
```cmd
ScreenshotBot.exe
```

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Приветствие и список команд |
| `/screenshot` | Полный скриншот экрана |
| `/windows` | Список окон с кнопками для быстрого скриншота |
| `/window Chrome` | Скриншот окна по названию (частичное совпадение) |
| `/help` | Справка |

## Оптимизация изображений (для замедленного трафика)

Когда Telegram замедляют и режут трафик, крупные PNG-файлы не проходят. Бот решает это так:

1. **Формат JPEG** вместо PNG — в разы меньше при том же визуальном качестве
2. **Автоподбор quality** — начинает с 90, снижает до 20 с шагом 10, пока не влезет в `max_size_kb`
3. **Уменьшение разрешения** — если при минимальном quality всё ещё не влезает, уменьшает картинку на 25% (до 3 раз)
4. **Ретрай при таймауте** — если отправка упала по таймауту, снижает лимит на 20% и пробует снова (до 3 попыток)

### Настройка через `settings.ini`

```ini
[image]
max_size_kb = 150
```

- `150` — стандарт для замедленного Telegram
- `100` или `80` — если 150 всё ещё не проходит
- `50` — агрессивное сжатие, когда совсем плохо с трафиком

Бот выводит текущий лимит при запуске:
```
📦 Лимит размера изображений: 150 КБ
```

## Структура проекта

```
Telegram_ScreenShoter/
├── screenshot_bot.py        # Основной код бота
├── settings.ini             # Конфигурация (создаётся вами)
├── requirements.txt         # Зависимости pip
├── Pipfile                  # Зависимости pipenv
├── README.md                # Этот файл
├── README_Python.md         # Подробности для разработчиков
├── README_EXE.md            # Инструкция для готового EXE
└── dist/
    ├── ScreenshotBot.exe    # Собранный EXE
    ├── ScreenshotBot.bat    # Лаунчер
    └── settings_exemple.ini # Шаблон настроек
```

## Сборка EXE

```bash
pipenv shell
python -m PyInstaller --noconfirm --clean --onefile \
    --name ScreenshotBot \
    --hidden-import win32timezone \
    --collect-all aiogram \
    --collect-all PIL \
    --collect-all magic_filter \
    --paths . screenshot_bot.py
```

Результат: `dist/ScreenshotBot.exe` — положите `settings.ini` рядом и запускайте.

## Устранение неполадок

| Проблема | Решение |
|----------|---------|
| Таймаут отправки | Уменьшите `max_size_kb` в settings.ini (100, 80, 50) |
| Ошибка 258 | Окно защищено UAC — запустите бот с теми же правами |
| screen grab failed | RDP свёрнут — держите сессию открытой |
| Бот не запускается | Проверьте `bot_token` в settings.ini |
| Антивирус блокирует | Добавьте папку бота в исключения |

## Системные требования

- **ОС:** Windows 10/11
- **Python:** 3.8+ (рекомендуется 3.11)
- **Сеть:** Интернет для Telegram API

## Лицензия

Код предоставляется «как есть» для образовательных целей. Используйте ответственно и соблюдайте законы о конфиденциальности.
