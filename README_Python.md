# Telegram Screenshot Bot - Python Development

## Описание
Telegram бот для создания скриншотов экрана и конкретных приложений с удаленным управлением через Telegram. Поддерживает интерактивный выбор окон через кнопки и устойчивый захват даже при перекрытых окнах.

## Основные возможности
- 📸 Полные скриншоты экрана
- 🪟 Скриншоты отдельных окон приложений (с интерактивными кнопками)
- 📋 Список открытых окон с удобным выбором
- 🔒 Система безопасности с контролем доступа
- ⏰ Автоматическое именование файлов с временными метками
- 🖥️ Устойчивый захват окон (PrintWindow + фолбэки)
- 🚀 Корректное завершение при закрытии крестиком
- 🔧 Настройки через settings.ini

## Установка и настройка

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd Telegram_ScreenShoter
```

### 2. Установка pipenv и зависимостей
```bash
# Установите pipenv, если не установлен
pip install pipenv

# Установите зависимости из Pipfile
pipenv install

# Активируйте виртуальное окружение
pipenv shell
```

### 3. Создание Telegram бота
1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Выберите имя и username для бота
4. Скопируйте полученный токен

### 4. Получение своего Telegram ID
1. Напишите @userinfobot в Telegram
2. Отправьте команду `/start`
3. Скопируйте ваш ID

### 5. Настройка конфигурации
Создайте файл `settings.ini` в корне проекта:
```ini
[telegram]
# Токен бота от BotFather
bot_token = 1234567890:ABCDEFghijklmnopqrstuvwxyzABCDEFghijk

# Список разрешенных пользователей через запятую (можно оставить пустым)
# Пример: 123456, 987654321
allowed_users = 123456789, 987654321
```

## Запуск

### Разработка
```bash
# Активируйте окружение
pipenv shell

# Запустите бота
python screenshot_bot.py
```

### Продакшн (без pipenv)
```bash
# Установите зависимости напрямую
pip install -r requirements.txt

# Запустите бота
python screenshot_bot.py
```

## Команды бота

### /start
Приветственное сообщение и список команд

### /screenshot
Создает полный скриншот экрана и отправляет в чат

### /windows
Показывает список открытых окон с интерактивными кнопками:
- Нажмите на кнопку с названием окна для мгновенного скриншота
- Поддерживается до 25 окон одновременно

### /window название_окна
Создает скриншот конкретного окна по названию
Примеры:
- `/window Chrome` - скриншот браузера Chrome
- `/window Notepad` - скриншот блокнота
- `/window Total Commander` - скриншот файлового менеджера

### /help
Показывает справку по командам

## Архитектура и особенности

### Устойчивый захват окон
- **PrintWindow API**: захват без активации окна (не меняет фокус)
- **Безопасный подъем**: окно поднимается наверх без изменения размеров
- **Множественные попытки**: до 3 ретраев с разными методами
- **Фолбэки**: полный снимок + кроп, затем региональный скриншот

### Корректное завершение
- Обработка закрытия крестиком (CTRL_CLOSE_EVENT)
- Автоматическая очистка ресурсов Telegram
- Безопасное завершение FSM-хранилища

### Поддержка мультимониторов
- Учет виртуального экрана Windows
- Корректная работа с отрицательными координатами
- Поддержка конфигураций "Расширить эти экраны"

### Конфигурация
- Настройки в `settings.ini` (не в коде)
- Автопоиск конфига рядом с exe/в _MEIPASS
- Поддержка PyInstaller onefile/onedir

## Разработка

### Структура проекта
```
Telegram_ScreenShoter/
├── screenshot_bot.py      # Основной файл бота
├── settings.ini           # Конфигурация
├── requirements.txt       # Зависимости для pip
├── Pipfile               # Зависимости для pipenv
├── Pipfile.lock          # Зафиксированные версии
├── README_Python.md      # Этот файл
├── README_EXE.md         # Инструкция для EXE
└── .gitignore            # Игнорируемые файлы
```

### Добавление новых команд
```python
async def new_command(self, message: Message):
    """Новая команда"""
    if not self.check_user_access(message.from_user.id):
        await message.reply("У вас нет доступа к этому боту.")
        return
    
    # Ваш код здесь
    await message.reply("Результат команды")

# В __init__ добавьте:
self.dp.message(Command("newcommand"))(self.new_command)
```

### Тестирование
```bash
# Запуск в режиме отладки
python -u screenshot_bot.py

# Проверка зависимостей
pipenv check

# Обновление зависимостей
pipenv update
```

## Устранение неполадок

### Ошибка "screen grab failed"
**Причина**: Свернуто/закрыто окно RDP или заблокирован рабочий стол
**Решение**:
- Держите RDP-клиент открытым и видимым во время съемки
- Или переведите сеанс на локальную консоль: `tscon <ID_сессии> /dest:console`

### Ошибка 258 "Время ожидания операции истекло"
**Причина**: Окно защищено UAC, запущено с другими привилегиями
**Решение**:
- Разверните окно на главный монитор
- Запустите бот и приложение с одинаковыми правами

### Проблемы с pipenv
```bash
# Пересоздание окружения
pipenv --rm
pipenv install

# Установка конкретной версии Python
pipenv --python 3.11
pipenv install
```

### Проблемы с правами доступа
- **Windows**: Запуск от имени администратора может потребоваться
- **macOS**: Системные настройки > Безопасность > Конфиденциальность > Запись экрана

## Сборка EXE

### Требования
- Активированное pipenv окружение
- PyInstaller: `pipenv install pyinstaller --dev`

### Команда сборки
```bash
# Активируйте окружение
pipenv shell

# Соберите EXE
python -m PyInstaller --noconfirm --clean --onefile --name ScreenshotBot --hidden-import win32timezone --collect-all aiogram --collect-all PIL --collect-all magic_filter --paths . screenshot_bot.py
```

### Результат
- `dist/ScreenshotBot.exe` - готовый исполняемый файл
- `settings.ini` - положите рядом с EXE

## Системные требования
- **ОС**: Windows 10+ (тестировано на Windows 10/11)
- **Python**: 3.8+ (рекомендуется 3.11)
- **Права**: Доступ к экрану и окнам приложений
- **Сеть**: Интернет-соединение для Telegram API

## Зависимости (Pipfile)
```
[packages]
aiogram = "==3.4.1"
pyautogui = "==0.9.54"
pygetwindow = "==0.0.9"
Pillow = "==10.2.0"
psutil = "==5.9.8"
pywin32 = "==306"
pywin32-ctypes = "==0.2.2"

[dev-packages]
pyinstaller = "*"
```

## Лицензия
Код предоставляется "как есть" для образовательных целей.
Используйте ответственно и соблюдайте законы о конфиденциальности.
